{
  "name": "ai-driven-sdlc-e2e",
  "private": true,
  "description": "Playwright end-to-end and API tests for the AI-DRIVEN-SDLC workflow (Phase 6: Test)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report",
    "playwright:install": "playwright install --with-deps",
    "playwright:agents": "playwright init-agents --loop=claude"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.0.0"
  }
}
