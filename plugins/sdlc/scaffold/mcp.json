{
  "mcpServers": {
    "playwright-test": {
      "command": "npx",
      "args": ["playwright", "run-test-mcp-server"],
      "env": {
        "BASE_URL": "${BASE_URL:-http://localhost:4280}",
        "LOCAL_BASE_URL": "${LOCAL_BASE_URL:-http://localhost:4280}"
      }
    }
  }
}
